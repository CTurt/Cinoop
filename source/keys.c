#include "keys.h"

struct keys keys;

/*int x;
for(x = 0; x < 8; x++) {
	int y;
	for(y = 0; y < 8; y++) {
		framebuffer[x + y * 160].r = palette[tiles[keys.keys1 / 10][x][y]].r;
		framebuffer[x + y * 160].g = palette[tiles[keys.keys1 / 10][x][y]].g;
		framebuffer[x + y * 160].b = palette[tiles[keys.keys1 / 10][x][y]].b;
		
		framebuffer[x + 8 + y * 160].r = palette[tiles[keys.keys1 - (keys.keys1 / 10) * 10][x][y]].r;
		framebuffer[x + 8 + y * 160].g = palette[tiles[keys.keys1 - (keys.keys1 / 10) * 10][x][y]].g;
		framebuffer[x + 8 + y * 160].b = palette[tiles[keys.keys1 - (keys.keys1 / 10) * 10][x][y]].b;
	}
}*/
